import { IframeResizerDirective } from './iframe-resizer.directive';

describe('IframeResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeResizerDirective(null);
    expect(directive).toBeTruthy();
  });
});
